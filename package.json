{
  "name": "openclaw-superchat",
  "version": "0.1.0",
  "description": "Superchat channel plugin for OpenClaw (single-contact mode)",
  "type": "module",
  "scripts": {
    "build": "esbuild index.ts setup-entry.ts src/channel.ts src/client.ts src/tools.ts --outdir=dist --outbase=. --format=esm --platform=node"
  },
  "openclaw": {
    "extensions": ["./index.ts"],
    "setupEntry": "./setup-entry.ts",
    "channel": {
      "id": "superchat",
      "label": "Superchat",
      "blurb": "Connect OpenClaw to a Superchat inbox (WhatsApp, SMS, email) via the Superchat Public API."
    }
  }
}
